# TECH_API_JSON_SPEC_v1.md 模板

- 文档类型：基线文档
- 当前状态：active
- 当前版本：v1.0
- 最后更新时间：[YYYY-MM-DD]
- 当前负责人：[项目负责人姓名 / AI / Codex]
- 是否为当前有效版本：是

---

# [项目名称] API + JSON 数据结构详细定义文档 v1

## 1. 文档信息
- 文档名称
- 适用阶段
- 文档用途
- 目标

## 2. 设计原则

### 2.1 接口设计原则
1. 前后端解耦
2. 内部能力可替换
3. 先结构化，后自然语言
4. 统一返回格式
5. 向后兼容优先

### 2.2 MVP 接口边界原则
- 本期纳入哪些接口
- 本期不纳入哪些接口

## 3. 通用约定

### 3.1 Base URL
```text
https://api.example.com
```

### 3.2 API 版本
```text
/api/v1/
```

### 3.3 Content-Type
#### JSON 接口
```http
Content-Type: application/json
```

#### 文件上传接口
```http
Content-Type: multipart/form-data
```

### 3.4 统一响应格式
```json
{
  "success": true,
  "code": 0,
  "message": "ok",
  "data": {}
}
```

### 3.5 时间格式
- ISO 8601

### 3.6 ID 规范
- 全部使用字符串 ID

## 4. 业务实体模型

### 4.1 实体总览
- [实体 1]
- [实体 2]
- [实体 3]

## 5. 核心 JSON 数据结构定义

对每个核心对象建议都写以下内容：
- 说明
- JSON 结构
- 字段说明
- 枚举值（如有）
- 坐标规则 / 排序规则（如有）

### 5.1 [EntityA]
#### 说明
#### JSON 结构
```json
{}
```
#### 字段说明

### 5.2 [EntityB]
#### 说明
#### JSON 结构
```json
{}
```
#### 字段说明

### 5.3 [EntityC]
#### 说明
#### JSON 结构
```json
{}
```
#### 字段说明

## 6. API 详细定义

### 6.1 [接口 1]
#### 接口说明
#### 路径
#### 请求类型
#### 请求字段 / 请求体
#### 成功响应示例
#### 失败响应示例

### 6.2 [接口 2]
#### 接口说明
#### 路径
#### 请求类型
#### 请求字段 / 请求体
#### 成功响应示例
#### 失败响应示例

### 6.3 [接口 3]
#### 接口说明
#### 路径
#### 路径参数
#### 成功响应示例
#### 处理中响应示例（如适用）

## 7. 错误码定义

### 7.1 错误码设计原则
- `0` 成功
- `1xxx` 请求错误
- `2xxx` 资源或状态错误
- `3xxx` 业务 / 分析 / 核心逻辑错误
- `5xxx` 服务端错误

### 7.2 错误码表
| code | message | 说明 |
|---:|---|---|
| 0 | ok | 成功 |

## 8. 前后端对接约束

### 8.1 前端必须依赖的核心字段
- [字段 1]
- [字段 2]

### 8.2 前端容错策略
- [策略 1]
- [策略 2]

### 8.3 后端兼容策略
- 新增字段优先追加
- 核心消费层尽量稳定

## 9. 调试与日志字段建议

### 9.1 timing 字段
```json
{
  "timing": {
    "total_ms": 0
  }
}
```

### 9.2 debug_info 字段（仅开发环境）
```json
{
  "debug_info": {}
}
```

## 10. 推荐开发顺序（面向 Codex）
1. 先搭接口骨架
2. 如需验证页面交互，可用 Mock 结果打通流程；若关键不确定性是接口真实行为，先做相称的真实验证
3. 再逐步替换内部实现

## 11. 最小 Mock 示例
```json
{
  "success": true,
  "code": 0,
  "message": "ok",
  "data": {}
}
```

## 12. 结论
[说明本版 API 文档最重要的工程原则]
