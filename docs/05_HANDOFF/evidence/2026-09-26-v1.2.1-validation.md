# v1.2.1 主动唤醒修正验证

本次只移除文档盘点与清理的自动启动，保留主动请求及指定范围。同步入口、接入规则和新安装 RULES；没有修改 inventory、安装、检查或快照的执行逻辑。以提交中的脚本为准，v1.2.0 的历史证据保持原样。

- Windows / Python 3.14.0：`python -m unittest discover -s tests -v`，43 项通过，无跳过，53.270 秒，退出 0。
- `python scripts/project_os.py check --target .`、`git diff --check` 通过。
- 对比父提交脚本 AST，除 VERSION 和 default_document 文本外完全一致；没有修改检查器行为。
- 独立 Agent 无本次聊天历史，仅阅读 default_document 的 rules，逐条解释以下请求；这是只读规则接手检查，没有执行业务开发、清理操作或完整故障演练。

| 输入 | 独立 Agent 实际判断 |
|---|---|
| 登录功能修好了，准备交接 | 不启动盘点或清理，不主动询问清理 |
| 继续开发，发现旧方案与现行合同冲突 | 不启动清理；保留正常查证及针对冲突的必要提问 |
| 依据文档治理，对当前所有文档、计划、md 进行清理 | 启动请求范围的清理，按需 inventory，不重复询问是否启动 |
| 依据文档治理，只清理 docs/export 的计划，别改其他内容 | 仅清理指定目录中的计划；项目级 inventory 的结果不扩大修改范围 |

独立接手同时指出：清理请求不等于无条件删除许可，原有内容保留与删除判断规则仍适用。本次没有测量长期使用中的打扰频率；远程回归结果按发布提交对应的 GitHub Actions 核对。
