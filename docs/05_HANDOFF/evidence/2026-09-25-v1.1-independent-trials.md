# Independent trial reports / 独立演练记录

These are reports from two separate agents started without parent conversation history. Fixture paths were provided, expected answers were withheld. The evaluator independently checked preserved file hashes and task outcomes. All projects, sessions and commits below are synthetic local fixtures. Relative fixture artifact names identify observations made during the trial; they are not files in this source repository. The fixture directories are excluded from distribution; the reports and evaluator assertions are retained here.

## Integration trial input

Integrate the source kit into this existing project. Reuse current rules and facts; preserve code, documents, uncommitted work, Git history and remotes. Follow README and the integration guide. Only the target may be edited. Do not use the network, external models, services, commit or push. Report actual commands and coverage. An independent handoff is a separate phase; do not count your own summary as blind recovery.

## Actual integration report

# Local governance adoption receipt

Result: local incremental adoption completed. Structural check passed without errors or warnings. This phase did not perform an independent handoff or grant business acceptance.

## Scope and timing

- Target: Tiny Notes, the existing local Python label-normalization library.
- Source: the separate `../../repository` toolkit checkout; treated as read-only.
- Started UTC: 2026-09-24T17:46:17+00:00.
- Finished UTC: 2026-09-24T17:50:22.305423+00:00.
- Elapsed: 245.3 seconds, including reading, installation, verification, and this receipt.
- Environment: Windows PowerShell, Python 3.14.0, Git 2.45.2.windows.1.
- Target HEAD remained `ef93fa2ddc872da210aa19bdb32bf64d9c500ed4`.
- Toolkit version: `1.1.0`; source HEAD `[development-source revision]`. The source checkout was dirty, so its commit alone does not identify the installed script. The exact copied script SHA-256 is `674a7e93b0c16c91598062c732036dc46e877fa2aef77312ded7786f73102a07` and the copied bytes matched the source during verification.

## Mapping and changes

Existing sources were reused: rules `AGENTS.md`, status `docs/CURRENT.md`, unfinished work `docs/HANDOFF.md`, decisions `docs/DECISIONS.md`. Tasks and evidence map to `docs/tasks` and `docs/evidence`. No second set of current-state documents was created.

The installer added only the manifest, local checker, mapped directories, and managed entry blocks. `CLAUDE.md` now imports `AGENTS.md` while preserving its existing text. A evidence link was appended to `docs/CURRENT.md`; `docs/tasks/.gitkeep` retains the otherwise empty tasks directory when tracked. No task was represented as accepted. The original project goal, scope, unknown Unicode behavior, and unrecorded business acceptance remain authoritative in `docs/CURRENT.md`.

## Actual commands and outcomes

All commands ran in the target root. Raw command arrays, stdout, stderr, and exit codes are retained in the linked JSON records.

| Actual command | Exit | Observation | Evidence |
|---|---:|---|---|
| `python ../../repository/scripts/project_os.py plan --target . --mapping work/adoption.json --json` | 0 | Existing sources reused; 6 planned changes; no conflicts | plan (fixture artifact: adoption-plan.json) |
| `python ../../repository/scripts/project_os.py apply --target . --mapping work/adoption.json --json` | 0 | Incremental writes completed | apply (fixture artifact: adoption-apply.json) |
| `python scripts/project_os.py check --target . --json` | 0 | No errors or warnings; harness `not_exercised` | check (fixture artifact: check.json) |
| `python ../../repository/scripts/project_os.py plan --target . --mapping docs/evidence/adoption-2026-09-25/adoption.json --json` | 0 | 0 changes; all operations reuse existing paths | repeat plan (fixture artifact: repeat-plan.json) |
| `python -B -m unittest discover -s tests -v` | 0 | 3 existing tests passed: repeated whitespace, whitespace-only empty result, case preservation | tests (fixture artifact: unit-tests.json) |
| `python scripts/project_os.py snapshot --target . --json` | 0 | Current mapping and Git observations captured | snapshot (fixture artifact: snapshot.json) |
| `git diff --check` | 0 | No whitespace errors | diff check (fixture artifact: diff-check.json) |

The mapping input and initial baseline were moved from temporary `work/` into this retained evidence directory. Temporary `work/` was then removed.

## Preservation verification

The inline Python preservation audit exited 0. It compared SHA-256 hashes and original byte prefixes against the pre-install baseline (fixture artifact: adoption-baseline.json). All 11 original files retained their content: 8 files are byte-identical and the other 3 retain every original byte as a prefix. Code, tests, README, decisions, handoff, `.gitignore`, and the user's untracked `scratch-note.txt` are byte-identical. `scratch-note.txt` is still untracked.

The same audit compared `git rev-parse HEAD`, `git show-ref`, `git rev-list --all`, `git remote -v`, and the hash of `.git/config`; every value matched the baseline. No commit or push occurred. Original origin remains `https://example.invalid/fixtures/tiny-notes.git`. See preservation results (fixture artifact: preservation.json) and tracked-file diff (fixture artifact: adoption.diff).

## Limits and next validation

- Independent new-session handoff: not tested in this phase; reserved for a separate agent/session. This installer's own reading and summary do not count as a blind handoff.
- Historical conflict, missing acceptance evidence, and interrupted worker recovery challenges: not tested in this phase.
- Harness task/receipt checks: not exercised; there are zero task JSON records.
- Business acceptance: not recorded by this integration. Existing unit tests are limited to their 3 documented cases; broader Unicode behavior remains unknown.
- Actual Claude Code startup and import loading: not tested. Only the compatibility entry text was checked.
- Repeated apply was not run; a fresh plan reports no changes. No automatic upgrade, uninstall, network, external model, paid operation, or service was exercised.
- No baseline comparison exists for reduced repeated work or user correction. This one local install does not establish effectiveness across projects.
- No user decision was required. Only the target was modified; no global configuration was changed.


## Recovery trial input

Continue this project's unfinished task. Start from AGENTS.md, establish the stopping point and evidence, then complete one locally verifiable next step. Only the target may be read or edited. Do not read evaluator or sibling directories, use the network, external models, real sessions, commit or push. Report actions, evidence and verification.

## Actual recovery report

# Recovery acceptance review

Started: 2026-09-24 17:51:15 UTC (2026-09-25 01:51:15 Asia/Shanghai).
Reviewer: Codex recovery reviewer, local synthetic trial.

## Entry and stopping point

Read AGENTS.md, project-os.json, and its mapped RULES, STATUS, HANDOFF and DECISIONS, then task normalize-1 and its linked receipt/evidence. The task was recorded as running despite an existing committed delivery. STATUS identifies this as an interrupted result-collection step. Historical notes are not the current task state.

The executor-runtime.json observation explicitly describes a synthetic fixture with running=false and writes_pending=false. No live worker or real session was queried or controlled. The executor message's claim of acceptance was treated as unverified.

## Independent verification

- Git root is the recovery-target directory.
- HEAD and receipt result revision both equal f0f4d3ce3bff6228168ffd75676d806d9a7f34c2.
- Baseline is ee2527797f66fb87645c7d9c834aed8da659e312.
- Inspected committed change: src/labels.py is the only changed file. `return text.strip()` became `return " ".join(text.split())`.
- This collapses repeated whitespace, strips edge whitespace and preserves letter case; no API or UI was introduced.
- `git diff <result_revision> -- src tests` returned no changes, so the locally tested source and tests match the delivery revision.
- Ran `python -m unittest discover -s tests -v` locally; exit code 0. Original captured output is in normalize-1-recovery-tests.txt. All three tests passed (internal whitespace, case preservation, empty whitespace).
- Initial dirty state comprised the existing AGENTS.md governance index plus untracked CLAUDE.md, docs/, project-os.json, project-os/ and scripts/. These were not mistaken for candidate implementation changes.

## Acceptance and preservation

Accepted the existing delivery after source inspection and fresh local testing. No new implementation, redispatch, external call, commit or push was needed. Preserved executor/session/worktree fields and original delivery checks. Retained original runtime observation, executor message and test log unchanged. Before-recovery task and receipt copies preserve the previous records.

Task lifecycle remains authoritative only in project-os/tasks/normalize-1.json. The receipt now links this independent verification. Project status and handoff point to that task instead of retaining a stale instruction to collect its delivery.

The project checker validates structure and local revision/evidence references only; it does not establish product correctness. Product acceptance here rests on the inspected source diff and independently executed unit tests.

## Final record verification

`python scripts/project_os.py check --target . --json` passed with no errors or warnings; harness state is accepted_records_checked (1 task, 1 accepted). Captured output: normalize-1-recovery-check.json.
Preservation assertions passed: task execution fields are unchanged, and all original receipt fields retain their original values. `git diff --check` passed; source and tests still match the delivered revision.
Finished: 2026-09-24 17:53:57 UTC.


## Evaluator context and limits

The recovery fixture contained a running task with an existing delivery, an explicitly historical NOT_STARTED note, an unverified worker message claiming acceptance, and synthetic stopped-worker metadata. The expected outcome was to inspect the existing delivery, ignore the obsolete instruction, reject the unsupported acceptance claim, then locally verify the candidate before accepting it. Business source and HEAD remained unchanged; the recorded session was retained.

The acceptance exercise does not prove real process recovery or general resistance to ambiguous conflicting evidence. The integration trial used the preceding checker build; after the diff-failure fix, the final checker was copied into both finished disposable fixtures and each structural check passed. The fix was covered by an actual failing-Git regression and the final 32-test run. See the sibling results JSON for final assertions and final checker hash.
