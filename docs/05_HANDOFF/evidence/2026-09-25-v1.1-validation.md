# v1.1 验证记录 / Validation record

日期：2026-09-25（Asia/Shanghai）。范围：本次工具包的增量接入、声明检查和隔离接续行为。环境：Windows、Python 3.14.0、Git 2.45.2.windows.1。

被测最终检查器的 LF 字节 SHA-256：`7e7f42f2fac3ceb037c7aecc5ecf3728e7e8a4550e54effc350eec9022e9e590`。本记录随候选版本提交；不将此前源 HEAD 冒充本次未提交实现的版本。

## 实际结果

| 检查 | 实际结果 | 限制 |
|---|---|---|
| 行为回归 | `python -m unittest discover -s tests -v`，32 项，40.623 秒，退出 0 / OK | 在本机 Windows 完成 |
| 母仓库结构检查 | `check --target . --json`，无错误、无提醒 | 无活动业务任务，harness 为 not_exercised |
| 独立 AI 接入 | 245.3 秒；复用四个既有来源，plan/apply/check/snapshot 通过，重复 plan 为 0 改动 | 使用虚构旧项目，不等于所有项目可自动适配 |
| 目标内容保留 | 代码、测试、README、用户未提交便签、Git config/HEAD/refs/history 保持；入口保留原字节前缀 | AI 在状态中追加本次证据索引，这是预期治理修改 |
| 独立 AI 接手 | 约 162 秒；识别已有交付，先检查并跑 3 项原测试，再登记验收；未重新实现、未派发外部任务 | runtime 元数据为合成夹具，没有真实 Claude 进程 |
| 过期状态与缺验收 | 未以明确标为历史的 NOT_STARTED 覆盖当前来源；未以工作者“全通过”消息替代验收 | 一次有明确来源的挑战，不证明任意冲突都可裁定 |
| 检查器审查 | Git diff 失败原先会跳过文件核对，已修复为 receipt_diff_unavailable；失败场景回归通过 | 不把日志存在当成日志真实性证明 |
| 可选技能 | 5 个 SKILL frontmatter / 命名检查通过 | 实际客户端自动发现和加载未测 |

机器回归覆盖：脏工作区保留、幂等、既有 AGENTS 映射、目录冲突预检、同源 / clone / worktree 误投、路径越界与链接、来源脱敏、错误字段类型、缺证据、错基线、不存在提交、越权文件、实际 diff、不完整验收命令，以及非当前 HEAD 的合法历史交付。

[结构化结果](2026-09-25-v1.1-results.json) · [两次独立演练的输入与报告](2026-09-25-v1.1-independent-trials.md)

## 未验证和使用边界

- 尚无真实项目持续使用的对照数据，不能声称已经降低返工率或提高开发效率。
- 未运行真实 Codex→Claude Code 派工、权限加载、会话恢复或付费操作恢复。
- `apply` 不是升级器；已安装工具需审查差异后合并。全写集预检不是跨文件事务或恶意竞态隔离。
- checker 不执行任务的验收命令，不验证日志真实性、验收者身份或业务语义。非 Git 的任务机器检查尚未实现。
- 续验：远程 Windows/Linux × Python 3.10/3.14 四组 CI 均通过，包含回归测试和母仓库结构检查；macOS 未实测。

后续使用按 [治理试验](../../03_DELIVERY/GOVERNANCE_TRIAL.md) 随实际任务留证。用户只需看结论和必要决策，不替 AI 逐项维护表格。
