# v1.1.1 更名与兼容验证 / Rename and compatibility validation

日期：2026-09-25（Asia/Shanghai）。本次范围：公开名称统一为 Awoo Vibe Coding Governance，接入地址改为 `https://github.com/furenzhong/awoo-vibe-coding-governance`，母仓库身份识别保留原 URL 的兼容性。

被测脚本 `scripts/project_os.py` 的 LF 字节 SHA-256：`101a6cf05cc5b12bfe524cf304e0b73d474df13e9932d19854791d1987151333`。

被测回归 `tests/test_project_os.py` 的 LF 字节 SHA-256：`b9838b9421c1b45f212df1b6aff3bbaebf1338b0f71f741834b59ef3101d35ab`。

## 实际检查

- Windows、Python 3.14.0：`python -m unittest discover -s tests -v`，34 项通过，42.336 秒，退出 0。完整输出见 [测试日志](2026-09-25-v1.1.1-tests.txt)。
- `python scripts/project_os.py check --target . --json`：通过，无错误、无提醒；源工具包没有活动业务任务，harness 为 `not_exercised`。
- 回归覆盖新旧名称的 HTTPS / SSH 地址、从 fork 接入时误选母仓库，以及旧来源记录遇到更名后的母仓库；拒绝误投，不改写既有来源记录。
- 中英文 README、安装命令、产品文档与接入提示词同步；相对 Markdown 链接检查和 `git diff --check` 通过。

## 兼容与证据边界

`project-os.json`、`project-os/`、`scripts/project_os.py`、入口标记与 schema 保留；已有项目不因更名迁移文件。`apply` 仍不是升级器，不会自动替换旧项目中的脚本；更新已安装版本时应比较并合并差异。

本次没有改动派工与恢复合同，也没有重跑独立 AI 演练。此前的 [v1.1 演练记录](2026-09-25-v1.1-validation.md) 只代表当时被测版本；真实跨工具进程恢复和长期使用收益仍未验证。

远程 CI 按提交运行 Windows / Ubuntu × Python 3.10 / 3.14；核对所用提交对应的 [Actions 运行记录](https://github.com/furenzhong/awoo-vibe-coding-governance/actions/workflows/check.yml)，不要用其他提交的绿色状态替代本次结果。
